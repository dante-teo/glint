# Repository Guidelines

## Project Structure & Module Organization

Glint is a macOS app built with SwiftUI, AppKit, and GhosttyKit. Application code lives under `Glint/`, grouped by feature: `App/` for app lifecycle and update wiring, `Chrome/` for shell UI, `Pane/` for terminal surfaces, `Agent/` for agent integration, `Workspace/` for persistence and workspace state, and `Ghostty/` for GhosttyKit management. Resources are in `Glint/Resources/`, including asset catalogs, entitlements, localization, themes, and `Info.plist`. Unit tests live in `GlintTests/`. Release and asset automation lives in `scripts/`; documentation and the static site live in `docs/`. `project.yml` is the XcodeGen source for the checked-in `Glint.xcodeproj`.

## Build, Test, and Development Commands

- `git submodule update --init --recursive`: fetch the `ghostty` submodule required by GhosttyKit setup.
- `scripts/setup-ghosttykit.sh`: download and verify the matching prebuilt `Vendor/GhosttyKit.xcframework`.
- `xcodegen generate`: regenerate `Glint.xcodeproj` after changing `project.yml` **or adding/removing source files**. XcodeGen resolves glob patterns at generation time, so new `.swift` files won't appear in the Xcode project until you regenerate.
- `xcodebuild test -project Glint.xcodeproj -scheme Glint -configuration Debug -destination "platform=macOS" CODE_SIGNING_ALLOWED=NO`: build and run the XCTest suite locally.
- `open Glint.xcodeproj`: open the app in Xcode for interactive development.

## Coding Style & Naming Conventions

Use Swift 5.10 conventions with 4-space indentation and clear feature-local organization. Prefer small pure helpers for domain logic, immutable values where practical, and SwiftUI views composed from focused private subviews. Name test files after the behavior under test, such as `AgentKindResolutionTests.swift`, and use direct test method names like `testUnknownReturnsNil`. Keep resources named by product or agent state, for example `CodexWorking.dataset`.

## Testing Guidelines

Tests use XCTest in the `GlintTests` target. Follow red, green, refactor: add or update a focused failing test before implementation changes. Keep tests independent and deterministic; use `@MainActor` for UI or workspace state code that requires it. Run the full `xcodebuild test` command before declaring a change complete.

To unit-test `WorkspaceStore` operations (tabs, splits, pane state), create a `WorkspaceStore()` and override `workspaces` and `selectedWorkspaceID` with test data. The init loads persisted state, but overwriting those two properties replaces it entirely. There are no live surface views in tests, so `focusedPaneLiveCwd()` falls back to the model — this is expected. See `CwdInheritanceTests.swift` for the canonical pattern.

Only apply `@MainActor` to test classes that actually require it (i.e. those calling `@MainActor`-isolated code like `WorkspaceStore` methods). Tests for non-isolated types such as `SleepAssertionManager` should remain unannotated. If a test file would need `@MainActor` for only some tests, split them into separate files — each named after the behavior it covers.

### Pane dim overlay and IOSurface compositing

The unfocused-pane dim wash (`Color.black` in `PaneView.paneBody`) must be **always present** in the ZStack with its opacity toggled (0 when focused, 0.18/0.28 when unfocused). Do NOT convert it to a conditional `if !isFocused { ... }` — that causes the SwiftUI layer to be inserted *after* the `GhosttySurfaceView`'s IOSurface-backed Metal layer is already composited, and the overlay ends up behind the surface (invisible). Keeping the overlay in the initial render guarantees correct Core Animation z-ordering. This applies to any SwiftUI overlay placed above an `NSViewRepresentable` with a GPU-backed layer in a ZStack.

### Split pane focus click propagation

`GhosttySurfaceView` (an NSView) handles `mouseDown` before SwiftUI's gesture system sees the event, so the `.onTapGesture { store.focus(paneID) }` on `PaneView`'s ZStack does NOT fire for clicks on the terminal surface. To propagate focus changes from clicks back to the store, `mouseDown` posts a `glintPaneFocusClicked` notification (same pattern as Esc/Return) with the pane key. The store observes this notification **synchronously** (`queue: nil` + `MainActor.assumeIsolated`) so that `focusedPane` updates before the next `updateNSView` pass can fight back and resign the first responder. Do NOT rely on `.onTapGesture` alone for focus — it only fires for clicks on SwiftUI-rendered areas, not on NSViewRepresentable content.

### Reacting to agent state changes

`paneAgentState` on `WorkspaceStore` is a `@Published` dictionary mutated from ~18 sites (subscript writes, `removeValue`, bulk `filter` reassignment). All mutations trigger its `didSet` hook. To add a new side effect driven by agent state (as `updateSleepAssertion()` does), add a call in the `didSet` — do not scatter calls across individual mutation sites, which is fragile and easy to miss. IOKit is already linked in `project.yml`.

### Agent workspace domain model

`WorkspaceKind` (`.terminal` | `.agent`) and `AgentProvider` (`.devin`) distinguish terminal from agent workspaces. Agent workspaces carry extra fields on `Workspace`: `kind`, `agentProvider`, `committed`, `agentProjectPath`, `agentSessionID`. All default to terminal-safe values (`.terminal`, `nil`, `true`, `nil`, `nil`) so old `state.json` files decode unchanged via `decodeIfPresent`. The encoder omits default values — terminal workspaces produce no extra JSON keys.

**Committed vs uncommitted:** Agent workspaces start `committed = false` (hidden from sidebar, stripped from `persist()`). Calling `commitAgentWorkspace(_:)` after the user sends their first message sets `committed = true`, making the workspace visible in `activeWorkspaces` and persistent across launches. Terminals are always committed.

**Auto-cleanup:** `selectWorkspace(_:)` silently removes the old workspace if it was uncommitted (user navigated away without sending a message). `addWorkspace()`/`addAgentWorkspace(provider:projectPath:)` also remove any prior uncommitted workspaces before creating a new one. All three call `closeAgentSessions(forWorkspaceID:)` before dropping the workspace from `workspaces`. This used to be a bare array removal on the assumption that "uncommitted workspaces cannot populate `agentSessions`" — that assumption broke once `AgentComposer.connectIfNeeded()` started eagerly starting a real ACP process as soon as an agent pane appears (see "Devin ACP protocol integration" below), before the user ever sends a first message and therefore before `commitAgentWorkspace(_:)` runs. If you add another path that discards an uncommitted workspace, call `closeAgentSessions(forWorkspaceID:)` first or you'll leak a live `devin acp` subprocess.

**Guards:** `splitFocused()` and `newTab()` beep and return for `.agent` workspaces (one session per workspace, v1). `liveIconKind(for:)` short-circuits to the provider's icon (`.devin`) for agent workspaces.

**Tests:** Store-dependent tests live in `WorkspaceKindTests.swift` (`@MainActor`). Pure Codable round-trip tests for the agent fields live in `WorkspaceAgentCodableTests.swift` (unannotated), following the `@MainActor` split convention documented above.

### Managed ACP agent core

Managed ACP panes are being refactored around provider-neutral state in `Glint/Agent/Core`; terminal-pane agent detection/control stays separate. `AgentSessionController` owns async lifecycle and UI-facing commands, while `AgentRuntimeAdapter` is the provider boundary. `DevinACPAdapter` is the first adapter and wraps the low-level `ACPClient`.

Keep domain projection in `AgentSessionReducer`, not in the controller: transcript chunk merging, tool upserts, status transitions, stale tool marking, pending permission/elicitation projection, usage/model/mode/config projection, and bounded transcript retention belong there. Keep project-root file safety in `AgentProjectFileService`, permission option selection in `AgentPermissionDecider`, and snapshot/journal persistence in `AgentConversationStore`. `DevinSessionManager` currently remains as a compatibility typealias; prefer new provider-neutral names for new code unless a call site still needs compatibility with existing UI/tests.

Architecture notes live in `docs/agent-architecture.md`. Update that doc when changing provider boundaries, reducer contracts, permission behavior, file access rules, or conversation persistence.

### Devin permission auto-review

`PermissionReviewMode` (`Glint/Agent/PermissionReviewer.swift`) has three user-facing modes: `.manual` ("Always Ask") always surfaces the permission card, `.autoReview` checks `PermissionPolicy.defaultPolicy(for:)` before falling back to the card, and `.alwaysAllow` selects an offered allow option without interrupting the user. In `.alwaysAllow`, never pick an arbitrary option: if Devin offers no allow option, respond with the ACP `cancelled` outcome. `PermissionPolicy.defaultPolicy(for:)` maps each `ToolKind` to `.autoApprove`, `.alwaysEscalate`, or `.llmReview`. `.llmReview` shells out to `devin -p -- "<prompt>"` (single-turn, non-interactive) with a prompt that ends in an unclosed JSON prefix (`{"decision":`) so the model can only continue with a valid JSON completion — `PermissionReviewer.parseResponse` re-prepends that prefix and decodes, with markdown-fence-stripping and regex-extraction fallbacks, escalating to the manual UI on any parse/timeout/process failure.

**Never overwrite `rawInput` to carry review metadata.** `RequestPermissionRequest.reviewReason` is a Glint-local field (excluded from `CodingKeys`, never encoded to or decoded from the ACP wire) added so the escalation path can attach the LLM's reasoning without destroying `rawInput` — the human reviewer must always see both the reviewer's analysis and the actual tool arguments on the permission card.

**Executable resolution:** `AgentPresence.executableURL(_:)` (`Glint/Agent/AgentHookInstaller.swift`) is the single shared resolver for locating a CLI agent binary (common GUI-safe install dirs plus `$PATH`, since a Finder-launched GUI app doesn't inherit the login shell's `PATH`). `ACPClient.devinExecutableURL()` and `PermissionReviewer`'s binary lookup both delegate to it — do not re-implement this search list again; call `AgentPresence.executableURL("devin")` (or the relevant binary name) instead.

**Tests:** Policy-only tests (no actor isolation needed) live in `PermissionPolicyTests.swift` (unannotated). `PermissionReviewer.parseResponse` tests live in `PermissionReviewerTests.swift` (`@MainActor`, since `PermissionReviewer` itself is `@MainActor`), following the same split convention as `WorkspaceKindTests.swift`/`WorkspaceAgentCodableTests.swift`.

### Devin ACP protocol integration

Glint integrates Devin through ACP v1 over the `devin acp` stdio subprocess (`Glint/Agent/ACP/ACPClient.swift`, `ACPTypes.swift`). Outbound requests use the ACP v1 shape: `protocolVersion = 1`, file read/write capabilities enabled, terminal capability disabled, `session/new`/`session/load` include `cwd` plus `mcpServers: []`, and `session/prompt.prompt` is an array of content blocks. If Devin rejects ACP v1 initialization, Glint surfaces a clear unsupported-protocol error instead of silently downgrading to an older wire format.

**Connecting and resuming (`AgentSessionController.connectIfNeeded(projectPath:)` / `establishSession`):** Glint starts the ACP process and resolves a session two ways. `sendPrompt`'s lazy path (`startSessionAndPrompt`) does it right before sending the first/next prompt, same as always. `AgentComposer.connectIfNeeded()` additionally calls `manager.connectIfNeeded(projectPath:)` from `.onAppear`/`.onChange(of: workspace.agentProjectPath)`, so the composer's model/mode/reasoning pickers have real data as soon as the pane is shown, not only after the user's first message. Both paths funnel through the same `nonisolated private static func establishSession(client:projectPath:existingSessionID:)`: if a persisted `sessionID` already exists (restored from a prior app run), it calls `session/load` to resume the *same* session first, falling back to `session/new` only if the Agent no longer recognizes it (expired, or the process restarted) or there was no existing id at all. Never call `session/new` unconditionally when a `sessionID` is already known — that silently abandons the previous conversation server-side even though the local transcript still shows it. `establishSession` must stay `nonisolated`: `AgentSessionController` is `@MainActor`, and static members of a `@MainActor` type are themselves MainActor-isolated by default (see the deliberately-`nonisolated` `selectOption`/`outcomeJSON` statics) — without it, the `client.start()`/`initialize()`/`loadSession`/`createSession` sequence would run on the main actor despite being invoked from `Task.detached`. Both connect paths report `status = .starting` while in flight, same as a first `sendPrompt`; `AgentPaneView`'s full-pane `loadingView` branch is gated on `case .starting = manager.status, manager.messages.isEmpty` specifically so a *resumed* session's `connectIfNeeded()` (transcript already loaded from disk) doesn't get its message list + composer swapped out for a spinner just because it's quietly reconnecting in the background — only a genuinely fresh session with nothing to show yet should see the spinner.

**`isConnecting` must be reset on every exit path, not just the happy one (another "stuck forever" class of bug).** `connectIfNeeded()` sets `isConnecting = true` before spawning its `Task.detached`, and the task only clears it via `finishConnecting()` — reached only when `isCurrentRun(runID) == true`. If the user hits Stop (or the pane closes) while still connecting, `stop()`/`prepareForClose()` bump `runID`, which fails that guard on the abandoned task and makes it return early. `stop()` and `prepareForClose()` therefore reset `isConnecting = false` themselves rather than relying on the async task to do it — if you add another flag that gates a `Task.detached` fenced by `runID`, reset it from every code path that can invalidate that run, not only from inside the task after its guard passes, or the flag can get stuck permanently and silently disable whatever it gates (here, `connectIfNeeded()` would never run again for that pane).

`ACPClient` routes newline-delimited JSON-RPC in one background stdout reader: responses resolve pending continuations by `id` (via `PendingRequest`, a lock-guarded single-resume wrapper — see below), notifications publish through `notifications`, and server requests dispatch to registered async handlers that write JSON-RPC results or errors. Malformed JSON lines are logged and ignored. Process termination, EOF, timeout, or cancellation fail affected pending continuations. Each `request(method:params:timeout:)` call races its own `timeoutTask` against the response (default 60s; `session/close` uses 2s; `session/prompt` uses 24h — a real agentic turn can legitimately run far longer than 60s, and cancellation, not a timeout, is the intended way to end one early) — do not add a second, redundant timeout layer on top of a single ACP call; if a caller needs a hard wall-clock bound across *multiple* concurrent calls, race an unstructured `Task` against a timeout `Task` and resume a shared continuation exactly once (see `WorkspaceStore.ResumeOnce`, which mirrors `ACPClient.PendingRequest`'s pattern) — a `withTaskGroup` alone does not bound execution time, since cancelling a task group's children only sets a flag and the group still awaits all of them before returning.

**Empty ACP responses:** for any request whose response body carries nothing Glint cares about (just "it didn't error"), decode as the existing `EmptyACPResult` (`ACPTypes.swift`) — e.g. `ACPClient.setMode`. Don't add another one-off `struct FooResponse { init(); init(from:) { ... tolerate null/object ... } }` type; several (`CloseSessionResponse`, `WriteTextFileResponse`, `KillTerminalResponse`, `AuthenticateResponse`) predate `EmptyACPResult` and are byte-for-byte duplicates of it — that's pre-existing debt, not a pattern to extend.

**Notification ordering (the "stuck tool call" class of bug):** `AgentSessionController.observeNotifications` delivers `session/update` notifications via `.receive(on: DispatchQueue.main)` followed by `MainActor.assumeIsolated { self?.handleNotification(...) }` — synchronously, with no further `Task { @MainActor in ... }` hop. This isn't just style: the `session/prompt` response for the same turn resolves its own MainActor hop via a separate async chain (`Task.detached` → `await markIdle()`), and an extra unstructured-Task hop on the notification side is not guaranteed to preserve relative ordering against it. Losing that race lets a late `toolCallUpdate`/thinking notification apply *after* `markIdle()`, leaving `status` stuck busy forever with the composer permanently disabled. If you add another notification-consuming subscriber anywhere in the agent stack, keep it synchronous once already on the main queue — don't wrap it in a fresh `Task`. Separately, turn activity gates status transitions so notifications the agent process keeps emitting for a turn the user already cancelled (`cancelTurn()`) can't flip `status` back to busy with nothing left to reset it; message/tool content from those notifications is still recorded either way.

**Message chunk merging only ever continues the tail of the transcript (the "answers render above questions" class of bug).** Devin commonly streams `agentMessageChunk`/`userMessageChunk`/`agentThoughtChunk` notifications with no `messageId`. `AgentSessionController.indexForChunk`/`appendChunk` (and the mirrored, not-yet-wired-in `AgentSessionReducer.indexForChunk`/`appendChunk` in `Glint/Agent/Core/AgentSessionReducer.swift` — see "Managed ACP agent core" above and `docs/agent-architecture.md`) must only append a chunk to the message currently at the *end* of the transcript array, never scan back through history for "the last message with a matching role." Scanning back lets a brand-new turn's reply keep gluing onto a much earlier turn's bubble as long as nothing else of that role happened to sit between them in the scan — even though a new user prompt (and possibly tool calls) had already been appended after it — which renders as a stale, ever-growing assistant bubble sitting *above* later user messages instead of a fresh bubble below them. If you touch either copy of this logic, keep the "tail only" invariant and update both copies together (see `testAgentChunksWithoutMessageIdDoNotMergeAcrossLaterTurns` / `testChunksWithoutMessageIdDoNotMergeAcrossLaterTurns`).

**Tool call transcript invariants:** Managed agent transcript rows intentionally preserve full ACP tool-call detail (`toolContent`/content, locations, raw input/output, timestamps, and stale/abandoned reason) rather than collapsing a tool call into only a display title. `AgentSessionReducer` is the provider-neutral upsert/projector for `tool_call` and `tool_call_update`; status-only updates commonly omit unchanged fields, so nil title/kind/content/location/raw fields must preserve existing values. A `tool_call_update` can legally arrive before the initial `tool_call`; create a placeholder row and fill fields later. If a turn ends, fails, closes, or is cancelled while any tool call is non-terminal, mark those tools stale instead of leaving the pane globally busy forever. The composer Stop button must call `cancelTurn()` (not `stop()`) so ACP receives `session/cancel`; `cancelTurn()` must also call `denyPendingInteractions()` so pending permission/elicitation continuations resume exactly once with the ACP `cancelled` outcome.

**Tool call UI and diagnostics:** `AgentToolCallRow` is an inspector, not a decorative status row. It should keep showing raw input/output, structured content/diffs, file locations, duration, stale reason, and copy actions when ACP provides them. The row uses a compact collapsed summary plus nested disclosures for status, locations, input, output, content/diffs, and raw JSON; keep heavy sections independently collapsible, copyable, and truncatable instead of flattening them into one large blob. Use `ACPJSONValue.prettyPrinted` for raw JSON display so objects are deterministic and top-level JSON primitives render as JSON fragments instead of Swift enum debug text. `ACPClient.diagnostics` publishes a bounded local event stream (requests, responses, notifications, server requests, stderr, malformed lines, lifecycle, errors); `DevinSessionManager.diagnostics` keeps only the recent in-memory tail for the pane diagnostics disclosure. Diagnostics are local debugging aids and are not persisted in conversation snapshots.

**Duration for a non-terminal tool call must be measured against live "now," never against `toolUpdatedAt`.** `AgentToolCallRow.durationLabel` only shows a frozen duration (`formattedDuration(from: toolStartedAt, to: toolCompletedAt)`) once the tool call is actually terminal; while it's still `pending`/`inProgress` it wraps the label in `TimelineView(.periodic(from: start, by: 1))` so the elapsed time keeps ticking against `context.date`. Comparing against `toolUpdatedAt` (the timestamp of whatever notification last touched the row) instead of live time was a real bug: a tool call that stops receiving updates — i.e. is genuinely stuck — would freeze at whatever tiny duration it had at that last update (e.g. `<1s`) forever, looking like it just started rather than like something a user needs to notice and cancel.

Devin compatibility is tolerant inbound decoding, not a second protocol mode. Glint encodes outbound `sessionId`, but accepts Devin's legacy `sessionID` alias for new-session responses, session notifications, permission/elicitation requests, file requests, and terminal requests. Unknown enum values and unknown `sessionUpdate` variants decode into `.unknown` cases so newer Devin messages don't break sessions. Per the ACP v1 spec, the `current_mode_update` notification uses a flat `modeId` key (`CurrentModeUpdate.modeId`) — distinct from `SessionModeState.currentModeId`, which is nested under `modes`/`configOptions` in session setup responses. Don't conflate the two field names.

**Permission request/response shape (was a real "stuck tool call" cause):** The ACP v1 spec's `session/request_permission` request nests tool details under `toolCall` (`{toolCallId, title?, kind?, status?, rawInput?}` — decoded here as `PermissionToolCallInfo`, deliberately more lenient than `ToolCallUpdate` since the spec's own example omits everything but `toolCallId`) and carries an `options: [PermissionOption]` array (`{optionId, name, kind}`) the agent is offering. The response **must** echo back `{"outcome": {"outcome": "selected", "optionId": "<one of the offered ids>"}}`, or `{"outcome": {"outcome": "cancelled"}}` if the client is abandoning the turn (mandatory when the user cancels/stops while a permission request is pending — see `denyPendingInteractions()`). `AgentSessionController.respondToPermission(approved:)` picks a real `optionId` from `pendingPermission?.request.options` via `AgentPermissionDecider`; `handlePermissionRequest`'s auto-approve/auto-deny paths and the `session/request_permission` handler's `self == nil` fallback all funnel through the same helper. `AgentPermissionDecider.selectOption` checks `allow_once`/`reject_once` before `allow_always`/`reject_always` *by kind*, not by the offered array's order (an agent can legally list `_always` before `_once`), and returns `nil` (→ `cancelled`) rather than falling back to `options.first` when no option of the requested direction exists. If you touch this path again, always decode/encode against the officially published schema at `https://agentclientprotocol.com/protocol/v1/tool-calls#requesting-permission` rather than assuming a shape.

**Composer plan mode vs. ACP mode/model switching:** The composer exposes only two local UX modes: `.agent` (default) and `.plan`. Plan mode can be enabled by typing `/plan` or by choosing Plan Mode from the plus menu; the UI shows a small Plan chip beside the approval selector. Do not call `session/set_mode` when the chip is toggled. Mode changes are folded into the next `session/prompt` text as `/plan` or `/agent`; exiting the Plan chip before `/plan` has been sent must clear the pending directive instead of queuing `/agent`. `ACPClient.setMode` and `AgentSessionController.selectMode(_:)` may still exist for protocol compatibility, but do not re-add a separate composer mode menu unless the product direction changes.

**Model/reasoning switching is real, confirmed, and lives entirely in `SessionConfigOption`, not the old `models`/`currentModel` fields.** The ACP v1 spec's *current, stable* mechanism for this is `session/set_config_option` (`https://agentclientprotocol.com/protocol/v1/session-config-options`), which supersedes the never-stabilized `session/set_model`: the Agent advertises a `SessionConfigOption` per selector (`category == "model"` for the model list, `category == "thought_level"` for reasoning effort) with a `currentValue` and an `options: [SessionConfigOptionValue]` array, and the client calls `session/set_config_option { sessionId, configId, value }` to change it, receiving back the complete updated `configOptions`. `ACPClient.setConfigOption`/`AgentSessionController.selectConfigOption(_:value:)` implement this (optimistic update, revert-on-failure), and `AgentComposer.modelMenu`/`reasoningOptions` render real, interactive menus sourced from `configOptions` — confirmed by driving `devin acp` directly over stdio and observing its `config_option_update` notification. The older unstable `models`/`currentModel` fields (and the composer's read-only fallback menu for them) are kept only in case a future/other provider never adopts config options; Devin itself no longer populates them on new sessions. If you touch this path again, decode/encode against the config-options schema above, not the removed unstable model API.

**Prompt content and attachments:** `ACPClient.prompt(sessionID:content:)` takes `[ContentBlock]` directly; a `prompt(sessionID:text:)` convenience lives in an `AgentRuntimeAdapter` extension (`content: [.text(text)]`) for simple callers. `AgentSessionController.sendPrompt(text:projectPath:images:)` builds the content array (text block, if any, then image blocks) from `AgentSessionController.ImageAttachment` (base64 data + MIME type). The composer's UI-facing `ComposerAttachment` (in `AgentComposer.swift`) additionally carries an `NSImage` thumbnail for display and is mapped down to `ImageAttachment` only at send time — keep that split; don't thread `NSImage` into the session controller.

`AgentConversationStore` persists conversation snapshots to `~/.glint/sessions/<workspaceID>.json` and append-only journals to `~/.glint/sessions/<workspaceID>.events.ndjson`. Snapshot saves clear the journal after the snapshot is written so replay cannot duplicate already-saved events. Saves are debounced ~250ms via `scheduleSave()`. `AgentProjectFileService` enforces selected-project-root file access for `fs/read_text_file` and `fs/write_text_file`: paths are standardized, symlinks resolved before the root check, reads must resolve to an existing non-directory file under the root, and writes validate the full resolved target (existing files) or the symlink-resolved parent directory (new files) — escapes through `..`, absolute paths, or symlinked directories/write-targets are rejected. Terminal server requests return not-implemented errors because Glint advertises `terminal = false`.

### Devin session teardown (archive, delete, quit)

Archive, delete, and app quit all route through `AgentSessionController` teardown methods built on a shared `prepareForClose()` helper (deny pending permission/elicitation, fence the run, detach the client): `closeSessionPreservingConversation()` (archive) closes the session and synchronously flushes the debounced save so a later "Resume Session" sees the final state; `closeSessionAndDeleteConversation()` (delete) closes the session, cancels any pending debounced save, and removes the conversation file; `closeSessionForQuit()` (app quit) *awaits* the ACP `session/close` call (which already self-bounds to ~2s in `ACPClient`) instead of firing-and-forgetting, then flushes. `WorkspaceStore.deleteWorkspace` also calls the static `DevinSessionManager.deleteConversation(workspaceID:)` compatibility API unconditionally for `.agent` workspaces, since a workspace restored from a prior launch whose pane was never opened this run has no live session to delegate the file removal to.

**`scheduleSave()`'s cancellation must be checked after the sleep, not assumed.** `Task.sleep` throws `CancellationError` on cancellation, but `try?` swallows that — without an explicit `guard !Task.isCancelled else { return }` right after the sleep, a "cancelled" debounced save still falls through and writes its (stale) snapshot, capable of silently resurrecting a just-deleted conversation file or clobbering a synchronous flush. If you add another cancellable delayed `Task` anywhere in this file, check `Task.isCancelled` explicitly after any `try?`-wrapped `Task.sleep` — don't rely on `.cancel()` alone.

`AppDelegate.applicationShouldTerminate` returns `.terminateLater` when `WorkspaceStore.hasLiveAgentSessions` is true, awaits `closeAllLiveAgentSessionsForQuit()`, then replies to actually terminate. `didConfirmViaWindowClose` only short-circuits the *busy-confirmation dialog* (so ⌘Q doesn't ask twice after a window-close confirmation) — it must still fall through to the graceful agent-shutdown step.

**Backlog:** Unarchiving lazily recreates the `AgentSessionController` on next pane open, which reloads the persisted conversation from disk — there is no dedicated read-only conversation view or separate "Resume" affordance beyond the existing sidebar context-menu item. Full ACP auth-method surfacing (beyond the current heuristic keyword match on failure messages) is also not yet implemented.

### Floating glass toolbar vs. non-terminal pane content

`ContentView.floatingHeader` (`store.glassEffect`) makes `ToolbarHeader` overlay the pane area instead of pushing it down in normal layout flow (Photos-style: terminal scrollback intentionally runs *under* the glass islands). Terminal panes stay clear of the toolbar via `GhosttyManager`'s padded shell launcher, not layout insets. Any plain SwiftUI pane content (e.g. `AgentPaneView`) has no equivalent trick, so its own top-anchored content (header, etc.) must add `ToolbarHeader.height` (52pt) worth of top padding whenever `store.glassEffect` is on, or it renders underneath the floating toolbar — visually, only whatever sits below the toolbar's bottom edge peeks out, which reads as a confusing partial UI rather than an obvious full overlap. If you add another non-terminal, top-anchored SwiftUI pane, give it the same `store.glassEffect ? ToolbarHeader.height : 0` conditional inset.

### AgentComposer dynamic sizing and message list width coupling

The composer's `TextEditor` auto-sizes to the draft's content instead of sitting at a fixed height. A hidden, invisible mirror `Text` (same font/padding, `.fixedSize(vertical: true)`) sits as a `ZStack` sibling of the real `TextEditor` purely to measure how tall the wrapped text wants to be, via a `GeometryReader` background reporting into `ComposerTextHeightPreferenceKey`. `onPreferenceChange` clamps that measurement through `AgentComposer.clampedEditorHeight(for:)` (pure, unit-tested in `AgentComposerEditorHeightTests.swift`) into `[minEditorHeight, maxEditorHeight]` (52...140pt — a real two-line minimum, not a fixed height) and animates `editorHeight`, which drives the real `TextEditor`'s `.frame(height:)`.

**The mirror `Text` must cap its own reported height, separately from the clamped `editorHeight` state.** A `ZStack`'s own size is the union/bounding box of all its children's sizes, and `.fixedSize(vertical: true)` makes the mirror report its true, *unbounded* height for however much text is pasted. Without an explicit `.frame(maxHeight: AgentComposer.maxEditorHeight, alignment: .top)` on the mirror itself — applied *after* the `GeometryReader` background, so the preference still measures the true, uncapped height — a long paste balloons the whole `ZStack`/card past `maxEditorHeight` via the invisible sibling, even though the visible `TextEditor` stays correctly capped through `editorHeight`. If you touch this measurement block again, keep that ordering: measure uncapped, then cap only what's reported upward.

`AgentComposer.maxWidth` (760pt) is the composer's floating card's max width (`AgentPaneView`'s `safeAreaInset` block) and is also used by `AgentPaneView.messageListSection` to cap/center the scrollable message list to the identical width. Both call sites must share this one constant — if the message list's width cap ever drifts from the composer's, right-aligned user bubbles will hug the full pane width while the composer stays centered/capped, and the two will visually overlap in wide panes.

**The composer card's background must have real opacity/blur, not a bare low-alpha tint.** It's painted via `.liquidGlass(enabled: store.glassEffect, cornerRadius: 16, tint: Theme.glassTint) { ZStack { VisualEffectBackground(material: .menu); Theme.overlay(0.06) } ... }` (same pattern as the command palette/toolbar islands), which gives a genuine `NSVisualEffectView` blur underneath. It used to be a flat `Theme.overlay(0.06)` fill with no blur at all — nearly transparent — so any layout timing hiccup (e.g. a same-tick resize, or the composer subtree remounting) let message text visibly bleed straight through the card instead of just being briefly mis-sized. Don't regress this back to a bare `Color`/`.overlay(_:)` fill.

**Every `Menu`-wrapped control in the toolbar row needs `.fixedSize()` after `.menuStyle(.borderlessButton)`.** SwiftUI's `Menu` on macOS can claim more width than its label content actually needs; without `.fixedSize()`, several `Menu`s side by side in an `HStack` (`plusMenu`, `modelMenu`, `approvalMenu`) read as huge, uneven gaps between controls instead of a tightly-packed cluster, even though the `spacing:` on the `HStack` itself is small. The toolbar row is laid out as two clusters — pickers (`plusMenu`, `modelMenu`, `approvalMenu`, the Plan chip) tightly packed on the left, one flexible `Spacer`, then icon-only actions (`contextUsageButton`, `sendButton`) on the right — mirroring the common "pickers left, actions right" composer layout (e.g. Codex's model/reasoning pickers + send button). If you add another `Menu` to this row, add `.fixedSize()` to it too or the gap problem comes back.

### App icon preset architecture

The Dock icon picker in Settings uses a two-level model: `PortraitStyle` (line art) x `IconColorTheme` (color palette) = `AppIconPreset` (the flat enum persisted to UserDefaults). The resolution helpers on `AppIconPreset` (`portraitStyle`, `colorTheme`, `preset(portrait:color:)`) map between the two representations.

**Naming asymmetry:** The original portrait uses bare color names as raw values (`sunrise`, `classic`, `aurora`, ...), while the other portraits use `<portrait>` for their sunrise/default variant and `<portrait>-<color>` for the rest (e.g. `bun`, `bun-classic`, `bun-aurora`). The `preset(portrait:color:)` resolver handles this branching. Do not assume a uniform `<portrait>-<color>` pattern for all presets.

**To add a new portrait style:** Add the base case and all color-variant cases to `AppIconPreset`, add a case to `PortraitStyle`, update the exhaustive switches in `portraitStyle` and `colorTheme`, add the corresponding `AppIconPreset-<name>` and `GlintLogo-<name>` image sets to `Assets.xcassets`, and run `xcodegen generate`. The compiler enforces exhaustive switches, so missing a case is a build error. The round-trip tests in `AppIconPresetGroupingTests` will catch any resolution/decomposition mismatch.

**To add a new color theme:** Add the themed cases for every portrait to `AppIconPreset`, add a case to `IconColorTheme` (including `swatchColor`), update the exhaustive switches, add image sets, and run `xcodegen generate`.

## Commit & Pull Request Guidelines

Commit history uses concise Conventional Commit-style prefixes, including `fix(agent): ...`, `feat(agent): ...`, `chore: ...`, `ci: ...`, and `release: ...`. Keep commits scoped to one change. Pull requests should include a short summary, tests run, linked issues when relevant, and screenshots or recordings for visible UI changes.

## Security & Configuration Tips

Do not commit signing secrets, Sparkle private keys, notarization credentials, or generated `.secrets/` material. Release setup is documented in `docs/releasing.md`; keep signing and appcast changes isolated from feature work.
